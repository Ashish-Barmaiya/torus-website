# Torus documentation content

Each MDX file mirrors an entry in `src/lib/docs/navigation.ts`. The route and navigation shell are intentionally separate from the content so the documentation can grow without creating bespoke page components.

The current pages are engineering placeholders: replace them with release-backed material while preserving their heading structure and metadata.
